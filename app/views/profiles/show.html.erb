<div class="profile_page">

  <%= content_for(:title) do %>
    <%= "Profile" %>
  <% end %>

  <div id="relative">
    &nbsp;
    <div class='text-center'>
    <%= link_to (image_tag @user.picture.url(:thumb), class: "img-circle pict_profile"), edit_account_path%>
    </div>
  </div>

  <div id="description">
    <div class="container">
      <div class="row">
        <div class="col-xs-12 ">
            <div class="text-center">
              <h2><%= @user.first_name %> <%=@user.last_name%> </h2>
            </div>

            <div class="text-center">
             <h4><%=@user.function%> at <%=@user.entreprise%> in <%=@user.city%></h4>
            </div>
            <div class="col-xs-8 col-xs-offset-2">
              <hr>
            </div>
            <div class="text-center col-xs-12">

              <p><%=@user.followers_count %> follower(s)</p>
              <br>
             <p><%= link_to "Follow", follow_profile_path(@user)%> | <%= link_to "Unfollow", unfollow_profile_path(@user)%></p>
            </div>
        </div>
      </div>
    </div>
  </div>


  <div id='profile_articles'>
    <div class="text-center">
      <h4> Articles</h4>
    </div>

    <% @user.articles.sample(3).each do |article| %>
      <div class="profile_article col-xs-4">
        <div class="profile_article_picture text-left">

          <%= link_to article_path(article) do %>
            <%= image_tag article.picture.url(:medium) %>
          <% end %>
          <hr>
          <p><%= article.title%></p>
        </div>
      </div>
    <% end %>
    <div class="text-right col-xs-12" >
        <p id='voir_plus'><%= link_to 'Voir plus' %></p>
      </div>

  </div>

  <br>
  <br>

  <% if @user.projects.count != 0 %>
    <div id='profile_projects'>
      <div class="text-center">
        <h4> Microgrids</h4>
      </div>

      <% @user.projects.sample(1).each do |project| %>
        <div class="profile_project col-xs-4">
          <div class="profile_project_picture text-left">
            <%= link_to project_path(project) do %>
              <%= image_tag project.picture.url(:medium) %>
            <% end %>
            <hr>
            <p><%= project.title%></p>
          </div>
        </div>
      <% end %>
      <div class="text-right">
        <p><%= link_to 'Voir plus' %></p>
      </div>
    </div>

  <% end %>
</div>
