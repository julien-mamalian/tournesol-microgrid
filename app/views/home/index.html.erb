<%= content_for(:title) do %>
  Home
<% end %>

<%= content_for(:description) do %>
  <%# TODO %>
<% end %>

  <div id="banner" class="window-height text-center" >
    <div id="animate-home">
      <h1 class="text-center"> Tournesol Microgrids</h1>
      <p> A Network of Microgrid pioneers</p>
      <br>
        <ul class="list-inline">
          <li>
          <%= link_to "View all articles", articles_path, class: "btn btn-success" %>
          </li>
          <li>
            <%= link_to "View all projects", articles_path, class: "btn btn-success" %>
          </li>
        </ul>

    </div>
  </div>
<div class="text-center padding-top-down big-font">
  Our Network is growing everyday !
</div>

    <div>
      <div id="map" class="big-white" style="width: 100%; height: 600px;"> TOURNESOL</div>

      <% content_for(:after_js) do %>
        <script>
          $(document).on('ready', function() {

            var mapOptions = {
              scrollwheel: false
            }

            handler = Gmaps.build('Google');
            handler.buildMap({ provider: mapOptions, internal: { id: 'map' } }, function(){
              markers = handler.addMarkers(<%= raw @markers.to_json %>);
              handler.bounds.extendWith(markers);
              handler.fitMapToBounds();
            });

          })
        </script>
      <% end %>
    </div>
  </div>
</div>
